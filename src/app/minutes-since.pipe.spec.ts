import { MinutesSincePipe } from './minutes-since.pipe';

describe('MinutesSincePipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesSincePipe();
    expect(pipe).toBeTruthy();
  });
});
